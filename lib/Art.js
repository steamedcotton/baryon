'use strict';
var colors = require('colors');

const intro =  '\n' +
               '──────────────────────────────────────'.white.bgBlue + '\n' +
               '               BARYON                 '.white.bgBlue + '\n' +
               '     simple lambda deployment cli     '.white.bgBlue + '\n' +
               '──────────────────────────────────────'.white.bgBlue + '\n';

module.exports = {
    intro
};